-- =============================================================================
-- 운용 조합(funds) 결성일(일단위) 추가
-- =============================================================================
-- 기존엔 결성 연도(vintage, int)만 저장 → 만기일처럼 일단위로 표시하기 위해
-- formation_date(date) 추가. vintage 는 결성일의 연도로 자동 도출돼 계속 유지
-- (정렬·드라이파우더 추정 폴백 호환). 재실행 안전(IF NOT EXISTS).

alter table public.funds
  add column if not exists formation_date date;

-- =============================================================================
-- SecondaryDesk — 투자사 등록 폼 간소화: investors.type 를 선택 입력으로 전환
-- =============================================================================
-- 배경: 등록 시 '투자사명'만 필수로 두기로 결정(제품 요구 변경). 유형은 선택값으로
-- 입력 없이도 저장 가능해야 하므로 NOT NULL 제약을 해제한다.
-- 적용: Supabase 대시보드 > SQL Editor 에 붙여넣고 실행.
-- =============================================================================

alter table public.investors alter column type drop not null;

-- =============================================================================
-- SecondaryDesk — 운용펀드 약칭(short_name)
-- =============================================================================
-- 운용펀드 전체명이 길어 화면 가독성이 떨어지므로, 약칭을 별도로 보관해
-- 화면 표시에는 약칭을 우선 사용한다(없으면 전체명 폴백). NULL 허용.
-- 재실행 안전(if not exists).
-- =============================================================================

alter table public.holding_funds
  add column if not exists short_name text;

comment on column public.holding_funds.short_name is
  '운용펀드 약칭 — 화면 표시용(없으면 name 폴백)';
